# Artifacts

schema: axiom_rift_artifacts_dir_v1
encoding: ascii_only

Generated reusable artifacts belong here when they are not raw data and are not local-only run output.
Large files are ignored by default. Register durable artifacts in `registries/artifact_registry.csv`.
